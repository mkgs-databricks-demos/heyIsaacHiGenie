# Use public npm registry for app container installs.
# The Databricks npm proxy is only for local development; the Apps platform
# runs npm install inside the container where the proxy is unavailable.
registry=https://registry.npmjs.org/
